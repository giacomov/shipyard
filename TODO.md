# TODO

- [ ] See if we can remove the dependency on the `gh` CLI (replace with direct GitHub API calls via e.g. `httpx` or `PyGithub`)
- [ ] Add a configuration or some other way to specify the default target branch for PRs (currently hardcoded to `main` in `execute.py`)
